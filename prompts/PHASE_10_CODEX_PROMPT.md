# Phase 10A Codex Prompt — Deployment Preparation

Read `AGENTS.md`, `README.md`, every document under `docs/`, the CI workflow, environment examples, and the completed Phase 1–9 implementation.

Prepare the controlled Rule Zero demo for a Vercel frontend and Render Python backend. Do not deploy, add credentials, or begin Phase 11.

Require production-only strong signing keys and exact non-localhost, non-wildcard CORS. Preserve the standard Next.js deployment, public demo routes, centralized frontend API timeout layer, deterministic backend, and all typed safety boundaries.

Add deployment configuration without secrets, an offline deployment validator, exact operator documentation, tests for fail-closed configuration, and release-readiness evidence. Run unit, lint, production build, backend, adversarial/configuration, practical Playwright, secret, whitespace, and validator failure-path checks. Review the complete diff for secret leakage, weakened boundaries, real-world actions, runtime automation, persistence, unsupported security claims, and Phase 11 work.
