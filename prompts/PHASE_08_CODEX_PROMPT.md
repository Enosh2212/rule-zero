# Phase 8 Codex Prompt — Guided Demo Experience

Implement only the Rule Zero Guided Demo and UI consolidation. Preserve `/demo/shopping` as the Security Lab and add `/demo` as a nine-stage, prerequisite-gated evaluator walkthrough.

Use only existing Phase 2–7 typed APIs. The frontend may coordinate display state but must not own policy decisions, canonical prices, controlled-state mutation, approval, execution, recovery, or audit verification. Require a distinct click for every proposal, evaluation, allowed execution, recovery plan/step, approval request/decision, payment evaluation, finish action, and audit verification.

Never auto-execute ALLOW, auto-approve ASK APPROVAL, override BLOCK, auto-run recovery, or add a one-click runner. Replay is read-only. Do not add an LLM, browser automation, authentication, persistence, real navigation, payment, submission, or sensitive-data transmission. Do not begin Phase 9.

Update landing, architecture, threat model, phases, build log, README, and `docs/GUIDED_DEMO_FLOW.md`. Test routes, controller gates/reset, explicit controls, complete mocked happy path, failure/audit warnings, responsive semantics, and Security Lab preservation. Run frontend tests/lint/build, backend pytest, `git diff --check`, and forbidden-functionality searches.
