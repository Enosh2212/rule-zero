NEXT_PUBLIC_API_URL=http://localhost:8000
# Only variables prefixed NEXT_PUBLIC_ are bundled into browser code. Never add signing keys here.
# In Vercel production, set this to the assigned HTTPS Render origin.
