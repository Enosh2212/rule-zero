ENVIRONMENT=development
CORS_ORIGINS=http://localhost:3000
APPROVAL_SIGNING_KEY=replace-with-a-strong-random-value
RECOVERY_SIGNING_KEY=replace-with-a-different-strong-random-value
AUDIT_SIGNING_KEY=replace-with-another-strong-random-value
# Production requires ENVIRONMENT=production, exact HTTPS frontend origins,
# and three independent strong keys. Wildcard and localhost origins fail closed.
